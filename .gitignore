
### IntelliJ IDEA ###
# Project-specific configuration directory
.idea/
# Module settings (should be ignored for Gradle/Maven projects)
*.iml

### Gradle & Build stuff ###
# Gradle cache and wrapper files
.gradle
# Build outputs (compiled classes, APKs, JARs)
**/build/
!src/**/build/
# Local configuration file (SDK location and potential secrets) - NEVER COMMIT
local.properties

### Android ### (tho doesn't apply here since there is no andriod target)
# Profiling and performance analysis data
captures
# Native C++ build files (generated by NDK)
.externalNativeBuild
.cxx

### Xcode (iOS/macOS) ### (tho doesn't apply here since there is no Apple target)
# User-specific data (breakpoints, window layouts, etc.)
xcuserdata
# Ignore all files inside the project bundle...
*.xcodeproj/*
# ...but keep the project structure and shared settings
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
# Keep workspace data
!*.xcworkspace/contents.xcworkspacedata
# Shared workspace settings
**/xcshareddata/WorkspaceSettings.xcsettings

### Compose Web / Kotlin JS ###
# Kotlin JS internal cache/store
.kotlin-js-store
# Node dependencies (for the web runtime)
node_modules/

### Kotlin ###
# Kotlin temporary files
.kotlin

### Mac OS ###
# Desktop Services Store (custom folder attributes)
# e.g. something that mac uses to store how the folders are located in icon view and things like that
.DS_Store

### Custom code and test stuff ###
# Pattern: Inside webMain source set, ignore any folder named 'testing' or 'Resources'
# regardless of the package structure (me/sasta/...).
src/webMain/**/testing/
src/webMain/**/Resources/




